# Backlog

Improvements agents noticed while building something else. One line each, with the task id that
spotted it. Nothing here is committed work — it is a list of candidates for the owner to promote
into `TASK_GRAPH.yaml` if worth doing.

Agents: add a line here rather than expanding your task's scope (AGENTIC_CONTEXT.md §1).

| Spotted by | Item |
|---|---|
| M6.6 | A6's rotation SELL journal entries (`analyst.rotation.engine.RotationEngine._decide`) carry rationale, sleeve and break-condition verdicts but **not** the trade's gross rupee value, realized gain, sell-side charges, or holding-period class — those live on the referenced `order_`/`Fill`, not the `decision_journal` row. The evidence pack's turnover/tax section (§5.7) is meant to be reconstructable from the journal alone, so it reads a SELL's value/gain from the payload (`value_inr`/`realized_gain_inr`/`charges_inr`/`holding_period`); a SELL without them is counted as an untraceable trade and surfaced as a defect. Have A6/A7's SELL path (and the deploy path's `deployed_inr`, for consistency, adopt the canonical `value_inr`) write these keys when it journals the trade, so real rotation exits populate the pack without the pack reaching into the order table. `_deploy_payload`/`RotationEngine._decide` are the seams. |
| setup | Object-storage target for L0 + Postgres backups is undecided; `ops/backup.sh` is local-only until one exists. |
| setup | Golden CA suite starts at 7 named cases; §4.3 expects ~13 more ugly cases collected during backfill. |
| M0.1 | `orchestrator/` is excluded from the gate (6 files unformatted, 7 mypy-strict errors). Bringing the build machinery under `make check` is a small, separate task. |
| M0.1 | The `platform/` → `dataplatform/` rename is documented in CLAUDE.md but ~30 `platform/…` path strings in TASK_GRAPH.yaml specs/deliverables still read the old name; a mechanical sweep would remove the indirection. |
| M8.2 | Regulation forces a daily broker API logout (NSE/INVG/73992 §8.3.2.1.8) — the daily loop needs a journaled `AUTH_REQUIRED` no-trade state alongside `SKIPPED_DATA_RED`; no task owns it. |
| M8.2 | Broker API terms are undated and change silently (kite.trade/terms preamble); a quarterly dated capture into `ops/compliance/` would make a term change detectable — C-track candidate. |
| M0.3 | This host runs its own Postgres on `127.0.0.1:5432`, so compose publishes the container DB on 5433 (ops/README.md). `.env.example` / `config.py` still default `DATABASE_URL` to `localhost:5432`, which reaches the wrong server from the host — either retire the host Postgres or move the default to 5433. |
| M0.2 | Closes the M0.3 row above: `DATABASE_URL` now defaults to `localhost:5433` to match what compose publishes. Flip both back to 5432 when the host Postgres goes away. |
| M0.2 | `configure_logging` sets up structlog only; httpx/uvicorn/apscheduler records still go wherever stdlib logging sends them, so an operator sees two log formats. Routing stdlib through structlog belongs with whatever owns the app entrypoint (M0.5). |
| C.1 | M1.2's acceptance covers spacing, backoff and the 403 hard stop but not payload validation. Three of nine sources answer unknown paths with HTTP 200 + HTML (`ops/gates/source-verification.md` §5.1), so a status-code-only fetcher will checksum markup into L0 as data — worth an explicit acceptance criterion on M1.2. |
| C.1 | `ListofScripData` defaults to `status=Active`, so a BSE identity backfill that uses the default silently imports survivorship bias. M3.1's spec does not mention pulling delisted/suspended scrips. |
| C.1 | EXECUTION_PLAN §4.1's Status column still reads `VERIFY-AT-BUILD` for rows the C.1 sweep verified. The register is now authoritative; the plan table was deliberately left untouched. A one-off sync would remove the discrepancy. |
| C.1 | Nothing re-verifies the register on a schedule. Endpoints drift (the niftyindices TRI gate is a live example) and today the first signal would be a daily-pipeline failure. A periodic sweep through M1.2's fetcher, reusing this register's evidence fields, would catch it earlier. |
| C.3 | Alert dedup state is per-process and lost on restart, so a crash-looping component alerts on every start. A journal-backed `last_alerted_at` table would make suppression survive restarts — worth doing once A9's journal exists. |
| C.3 | Nothing yet turns a FAILED streak, red quality or a recon break into an `Alerter.send` call; C.3 built the channel only. M1.2, D7 and X1's recon each own their own trigger, and no task owns the severity/dedup-key convention across them. |
| C.2 | `nse_holidays.yaml` coverage ends 2026-12-31 and `expected_sessions` refuses dates past it by design, so the daily job will hard-fail in Jan 2027 unless someone appends the new year. Nothing schedules that refresh from the NSE holiday master (fetchable, one request) — a December reminder or an auto-refresh task would close it. |
| C.2 | For 2016-2025 the calendar's weekday closures are verified but weekend-falling holidays are not enumerated (a non-session weekend is indistinguishable from a weekend holiday). Harmless for gap reporting; it would matter to anything that wants the exchange's declared holiday list verbatim. |
| M0.4 | Nothing applies migrations on container start: a fresh `make up` leaves the app talking to an unmigrated DB until someone runs `make migrate`. An entrypoint step (or a `/health` degradation when `schema_migrations` is behind the files on disk) would make the gap visible instead of latent — belongs with whatever owns the app entrypoint. |
| M4.4 | The 2017-2024 schedules in `execution/costs/rates.yaml` are reconstructed from secondary sources (only the current one is read off the published rate card). A single real contract note from 2018 and one from 2022 would let them be verified to the paisa; the residual uncertainty is paise per ₹50k trade, all of it in the exchange/SEBI/GST block. |
| M4.4 | Pre-2020 state-wise stamp duty is encoded for KA and MH only, and the states that capped duty per contract note (Telangana, Haryana) are not modelled at all — an unlisted state raises. Only matters if a pre-July-2020 backtest is run for an account registered elsewhere. |
| M4.4 | The rate card starts at 2017-07-01 (GST rollout), so a backtest reaching into the service-tax era raises rather than pricing. The 10-year backfill horizon crosses that boundary in mid-2016; a service-tax schedule (14% + SBC + KKC) would close it. |
| M0.7 | Nothing prunes `ops/backups/`, and nothing schedules `make backup` — it is hand-run until M0.6's scheduler owns the daily pipeline. Retention becomes a real decision the moment an object-storage target exists. |
| M4.4 | Only delivery equity has a rate card. Intraday, F&O, currency and commodity are deliberately absent; when a task needs them they belong in `rates.yaml` and `execution/costs/model.py`, never in a second module (invariant #4). |
| M1.2 | The fetcher deliberately does not validate payload shape (the C.1 row above): a soft-404 answering 200 + HTML is checksummed into L0 like any other body, which is correct for L0 — it stores what the source served — but means FETCHED is not the same as usable. The check belongs in M1.3's FETCHED→VALIDATED transition and in each parser's era check; no task owns it today. |
| M0.6 | Nothing runs the scheduler: `ops/docker-compose.yml` has postgres + the status API only, so `python -m dataplatform.scheduler run` is hand-started. The service belongs beside `app` (same image, different command) — natural to add with M1.11, when the EOD job stops being a no-op. |
| M0.6 | A job's `timeout` is a budget the runner reports on, not a kill (CPython cannot safely kill a thread mid-transaction). A genuinely hung job therefore holds its advisory lock for as long as it hangs and never runs again. A watchdog that alerts on a `job_run` row left `RUNNING` past its budget (C.3's channel exists) would make that visible; today only `/health` going stale would. |
| M0.6 | `job_run` grows one row per attempt forever and nothing prunes it. Harmless at one daily job; worth a retention decision alongside `ops/backups/` before the intraday announcement polls (§8.1) land. |
| M2.1 | `corporate_actions.action_type`'s CHECK in `0001_init.sql` predates the M2.1 taxonomy: it allows `CONSOLIDATION`/`OTHER` and rejects `SCHEME_OF_ARRANGEMENT`, `DVR_CONVERSION`, `NAME_CHANGE`, `BUYBACK` and `DELISTING`, so five of the twelve normalized types cannot be stored today. M2.2 is the first task to write these rows and should own the widening migration; M2.1 deliberately did not add one, having no rows to write. |
| M2.1 | Nothing persists the manual-entry queue — it lives for the duration of one parse run. M2.3 owns the reconciliation queue and its table; until then an unparseable purpose string is visible only as a `ca.purpose.queued` log event. |
| M0.5 | Integration modules each create a scratch database whose name is a module constant, and the build runs several agents against one Postgres — one session's `DROP DATABASE ... WITH (FORCE)` then kills another mid-test, which reads as flakiness (observed once during this task). Every integration module now keys the name by pid (`test_status_sync.py` was the last, fixed under M1.11 after it took that task's `make check` down). One shared fixture would still be better than five copies of the same constant. |
| M0.5 | `/archives?date=` returns the manifest but no download link, because the route that serves bundle files is M1.12's and public redistribution of exchange data is a HUMAN_GATE legal question (§10). When M1.12 lands, `ArchiveFileOut` is where a URL field belongs. |
| M5.1 | `ops/backup.sh` manifests `L0` only, so `data/evidence/` — the content-addressed bundles every journal entry points at — is outside the backup. The Postgres dump preserves the references and loses what they refer to, which makes a restored journal unreconstructable (§8.3.3). One more `find` block in the manifest, or a decision that evidence lives elsewhere, belongs with whoever next owns M0.7. |
| M5.1 | The evidence store has no integrity sweep: damage to a stored bundle is only found when something reads it, whereas `L0Store.verify_checksums` reports L0 damage proactively. The monthly evidence pack (§5.7) is the natural place to run one. |
| M5.1 | Write-once `O_EXCL` + `0o444` + fsync creation now exists twice, in `dataplatform/store/l0.py` and `analyst/journal/evidence.py`. The durability semantics are the interesting part and they should have one implementation; a small `dataplatform.store` primitive would do it without coupling A9 to L0's layout. |
| M5.1 | `TRUNCATE decision_journal` is refused by `order_`'s foreign key before the append-only trigger is reached; only `TRUNCATE ... CASCADE` exercises the trigger (both are asserted in `tests/integration/test_journal.py`). Harmless today, but a schema change that drops that FK would silently narrow what the plain form proves. |
| M1.4 | The NSE historical archive holds a *third* cash-market era before the one M1.4 parses: `cm04JAN2010bhav.csv` (fetched during M1.4, kept in L0, not checked in) has eleven columns and neither `TOTALTRADES` nor `ISIN`. `bhavcopy_legacy.parse` refuses that header rather than inventing an identity for rows that have none (invariant #2), and `source_register.yaml` still says `era.start: null` for the row. Nothing needs it today — the C.2 calendar only covers 2016-01-01 onwards, so no planned backfill reaches it — but a deeper history would need its own parser plus a symbol→ISIN resolution through D2, and the exact year the `ISIN` column appears is unpinned. |
| M1.11 | The gap report reads its L1 dataset name from the source id (`L1/<source>/date=.../`). M1.8 owns the real normalized-dataset naming; if it picks something else, every published date reports `NO_DATASET` and is counted as `l1_unchecked` rather than flagged — safe, but the L1 half of the report is then silently inert until the mapping is corrected in `dataplatform/quality/gaps.py`. |
| M1.11 | `ops/README.md`'s "Status of the app entrypoint" section still describes the pre-M0.5 skeleton serving only `/health`; the six §4.4 endpoints have been live since M0.5. One paragraph, belongs to whoever next touches ops docs. |
| M3.4 | `fii_dii.write_l1` writes its own Parquet partition because M1.8's shared `dataplatform/store/l1.py` does not exist yet. Fold it onto the shared writer when M1.8 lands — two partition writers is exactly the "one implementation" rule this repo keeps elsewhere. |
| M3.4 | Historical FII/DII is unobtainable from NSE (measured: one session, `source_register.yaml` `nse_fii_dii_flows.history`). The NSDL FPI portal and CDSL carry the past but are FPI-only, custodian-confirmed, and have no register entry or robots record. If the M3 gate's "10 years of flows" is to mean anything, that is its own ingestion task — with the caveat that it would be a different series, not this one backfilled. |
| M3.4 | Nothing schedules the daily flows capture yet, and a missed session on this feed is a permanent hole (no date parameter, no archive). M1.10 owns wiring `ingest_flows_day` into the EOD job; until it does, every day not run is history lost. |
| M3.4 | The 403-on-warm-up fix in `fetcher._warm_session` is exercised only through this task's tests. `tests/unit/test_fetcher.py` scripts the handshake as a 200 and so never sees the case NSE actually serves — worth a case there too, in M1.2's own file. |
| M5.3 | §5.7's journal vocabulary has no lifecycle member, so every case transition is filed as `POLICY_PROPOSAL` with `payload["event"]` naming what it really was. A dedicated `CASE_LIFECYCLE` decision would read better and make the evidence pack's decision review cleaner; it needs a migration altering `decision_journal`'s CHECK plus the matching enum in `analyst/journal/models.py`. |
| M5.3 | `make check` is repo-wide, but a wave runs several builder agents in one working tree — so an unformatted or half-typed file from another in-flight task fails everyone's gate, and the shared docker Postgres makes integration tests fail differently on every run. A worktree per task (or a scoped gate) would remove the cross-talk. |
| M1.7 | `symbolchange.csv` (`nsearchives.nseindia.com/content/equities/symbolchange.csv`) has no Source Register row: §4.1 row 6 names "name-change history files" but C.1's sweep only verified `EQUITY_L.csv`, and adding an entry now would need `sweep.swept_at` and the request counts moved — C.1's evidence record, not M1.7's. A real fetch was made and its evidence (200, 68,434 B, `text/csv`, sha256 `eee469d1…`) is recorded in `tests/fixtures/nse_equity_list/README.md`, so the next sweep can add the row without re-deriving it. |
| M1.7 | The identity master is NSE-only, so `security_master.status` is always ACTIVE: `EQUITY_L.csv` lists live securities and says nothing about delistings, and absence from one snapshot is not evidence of one (a truncated download looks identical). SUSPENDED/DELISTED land with M3.1's BSE scrip master, which publishes status explicitly — until then a point-in-time universe built from this table is survivorship-biased for anything that left NSE. |
| M1.7 | 52 of 2,886 symbol windows are clamped because NSE's `DATE OF LISTING` post-dates the rename chain it should precede (the listing date is the current entity's). Those securities resolve to "unknown" before their clamp date rather than to an ISIN. Accumulating weekly `EQUITY_L.csv` snapshots, or BSE's listing dates, would close most of them; `IdentityIngestReport.clamped` names every one. |
| M1.7 | `security_master.status` is last-writer-wins on re-ingest, so when the BSE master (M3.1) starts writing the same ISINs, whichever exchange was ingested last decides the status of a security suspended on one exchange and trading on the other. `exchange_listing.status` is already per-exchange and correct; the roll-up rule for the ISIN-level column is the open question. |
| M3.6 | The shareholding fixture (`tests/fixtures/nse_shareholding/json_v1/`) is a representative, hand-built payload, not a frozen live capture — `source_register.yaml` still says `fixture.frozen: false`. Tests never hit the network (B8), so the parser could not freeze the real 2.4 MB `corporate-share-holdings-master` response itself; a verify+sample pass (B1) should fetch it, freeze it, and flip `frozen: true`. |
| M3.6 | `source_register.yaml`'s `parse_check` for `nse_shareholding_pattern` lists the *master index* keys only (isin, name, date, broadcastDate, cgTimeStamp, pr_and_prgrp, employeeTrusts, publicShareholdingNotes, promoterDematNotes, industry) — which carry promoter holding and both dates but **not** pledge %, FII or DII splits. `shareholding.py` models pledge/FII/DII as first-class fields (BC3 needs pledge) and reads them from per-record keys (`pledgeShares_prcnt`, `fii_prcnt`, `dii_prcnt`); if the live master omits them, they come from the per-company detailed shareholding-pattern filing and this parser needs a second endpoint (or a join) to populate them. Confirm against a real capture during the B1 freeze above. |
| M3.6 | `shareholding.write_l1` writes its own Parquet partition (partitioned by `filing_date` for PIT), like `fii_dii.write_l1`, because M1.8's shared `dataplatform/store/l1.py` does not exist yet. Fold both onto the shared writer when M1.8 lands. |
| M3.6 | `read_pit` returns every filing knowable as of a date, including multiple filings of the same `(isin, period_end)` when a company restated a quarter. Picking the latest-knowable restatement (and quarantining restated data from backtests, §7 / invariant #8) is D3's job; until then a caller wanting one row per quarter must dedupe on `(isin, period_end)` by max `filing_date` itself. |
| M2.2 | CA ingestion built the parse→resolve→persist path (both parsers, the shared `CorporateAction` model + `write_corporate_actions`, migration 0005). No wired daily-forward/backfill *runner* exists yet (fetch→L0→parse→persist→sync_state, per-date for NSE and per-scrip for BSE, à la `nse/fii_dii.py:ingest_day`); acceptance did not require one and live backfill execution is the gated B1 go. Fold it into the M2 daily job / backfill campaign. |
| M2.2 | `CaParseResult` returns the manual-entry queue and an `unresolved` identity list per parse; both are logged but not persisted (M2.3 owns the reconciliation-queue table). M2.3 should drain `CaParseResult.queued`/`.unresolved` into that table with ex-date + source so a human sees them. |
| M2.2 | Corp-action fixtures reproduce the C.1-verified field schema but are hand-built, not captured response bodies (the sweep froze checksums, not bodies). Replace with real captured bodies per format era during the B1 verify-and-sample fetch, then re-freeze. |
| M3.5 | Deals ingestion built the parse→resolve→write→query path (`nse/deals.py`: both parsers, `DealsDay`/`write_l1`, `deals_for` for T0). No wired daily *runner* exists yet (fetch→L0→sync_state→L1, à la `nse/fii_dii.py:ingest_day`), and both endpoints are rolling one-session files with no history — depth accrues forward from first capture. Fold the runner into the M3 daily job; acceptance did not require it. |
| M3.5 | `normalize_client_name` is deliberately conservative (case-fold + whitespace-collapse only). Real files carry corporate-suffix and formatting variants (`M/S …`, `… LLP`, `… PRIVATE LIMITED`, a stray trailing `-`) that should be entity-resolved to a stable client id for T0's "same client accumulating" signal — a T0 concern (§5.4), not ingestion's. Build a client entity-resolution table there; the raw string is preserved in L1 so it can. |
| M3.8 | Announcements ingestion built the parse→resolve→write→search path (`ingest/announcements.py` for both exchanges, `AnnouncementBatch`/`write_l1`, `query/announcement_search.py` for T0). `ingest_nse`/`ingest_bse` drive one poll (fetch→L0→parse→L1→sync_state); the recurring intraday+EOD *scheduler* wiring (and formatting the NSE `from_date`/`to_date` and BSE `strPrevDate`/`strToDate` templates per poll date) is the M3 daily-job's, not this task's. Fold in when the scheduler lands. |
| M3.8 | `parse_bse` reads page 1's `Table` only. BSE pages announcements via `pageno`/`Table1`; a high-volume day spills onto later pages. The poll runner must iterate `pageno` until `Table` is empty and concatenate, then dedupe (`dedupe` already keys on `NEWSID`). Not exercised here — the fixture is one page. |
| M3.8 | Announcement fixtures reproduce the C.1-verified field schema (register `parse_check`) but are hand-built, not captured response bodies, and the register entries stay `frozen: false`. Replace with real captured bodies per exchange during the B1 verify-and-sample fetch, then set `frozen: true`. |
| M3.8 | `AnnouncementRow.symbol` is display-only and populated for NSE (native) but left `None` for BSE (feed keys on scrip). A report wanting the as-of BSE symbol should resolve it through D2 (`symbol_as_of(isin, ts.date(), exchange=BSE)`) at read time rather than storing it — kept out of the row to avoid a second identity fact to keep true. |
| M3.8 | `KeywordQuery` matching is whole-word-anchored prefix + phrase (morphology-lite, no stemmer). It is deliberately simple for T0; a thesis needing true stemming, synonyms, or proximity ("divest" NEAR "robotics") would want a richer matcher. Revisit if ratified break conditions outgrow the all_of/any_of/none_of shape. |
| M2.3 | The reconciliation queue built here holds *cross-exchange disagreements* (`ca_reconciliation` quality flags). The separate M2.2 note — draining `CaParseResult.queued` (unparseable purpose strings) and `.unresolved` (identity gaps) into a persisted queue — is still open: those are a different queue (a human enters terms / fixes identity, not adjudicates NSE-vs-BSE), and M2.3's acceptance did not cover them. Persist them alongside, keyed by ex-date + source, when the M2 daily runner lands. |
| M2.3 | `persist_reconciliation` dedupes flags by a `detail->>'fingerprint'` check-then-insert; `quality_flag` has no unique constraint on it, so two concurrent reconciliation runs could double-insert one disagreement. Harmless for the single-maintainer daily job (one writer), but add a partial unique index on `(check_name, (detail->>'fingerprint')) WHERE NOT resolved` if reconciliation ever runs concurrently. |
| M2.3 | Terms agreement is exact value equality: NSE `FaceValueTerms(10→2)` and a hypothetical BSE `FaceValueTerms(100→20)` are economically the same 5:1 split but land in the queue as a `RATIO_MISMATCH`. Conservative on purpose (a human sees the numeric difference), but if the feeds are found to routinely disagree on face-value scale, an economic-equivalence comparator (shared with the M2.4 factor arithmetic) could auto-reconcile them. |
| M1.12 | The daily bundle publishes only `prices_raw` (Parquet + CSV). As more L1/L2 datasets land (fo_aggregates, fii_dii, deals, shareholding, adjusted L2 series), extend `publisher._DEFAULT_DATASETS`/`_DATASET_SCHEMAS` so a bundle is the whole day's normalized surface, not just cash prices. Lineage is currently the date's full L0 ref set applied to every file; when a bundle spans datasets with different L0 provenance, narrow each file's lineage to the refs that dataset actually derived from. |
| M1.12 | `/archives` returns the manifest but not embedded per-file download links (the link is `/archives/download?date=&name=`, constructed by the caller). If a UI wants links in the payload, add an optional `download_path` to `ArchiveFileOut` (kept out of the DB manifest, computed on read) rather than storing a host-relative URL in the row. |
| M1.12 | No wired daily *publish* step exists yet — `publish_bundle` is a library call. Fold it into the M1 daily job (D6 after L1 is PUBLISHED and the gap check passes, per M1.10's spec) so a bundle is produced automatically each session. |
| M1.9 | The backfill runner ships one source set, `nse_bhavcopy` (cash prices → `prices_raw`, no delivery join). Delivery `%` (`nse_sec_bhavdata_full`, needs the D2 master), BSE bhavcopy, and other datasets are each a `SourceSet` in `backfill.py`'s `SOURCE_SETS`; add them when their daily pipelines land (M1.10/M3). The runner's fetch→L0→parse→L1→sync_state loop is dataset-agnostic, so each addition is a `SourceSet` entry, not new machinery. |
| M1.9 | `--dry-run` plans the full session set from the calendar and never touches the DB (so it needs nothing running); it does not subtract already-`PUBLISHED` dates. A `--dry-run` that optionally reads `sync_state` to show "N left of M" would help mid-backfill, but was kept DB-free so the plan/count is always safe to run. |
| M2.4 | Rights issues are not in the factor chain yet. A rights adjustment factor depends on the theoretical ex-rights price (TERP), which needs the cum-rights market close, so it is not a pure-terms ratio like a split/bonus; none of the seven §4.3 golden cases is a rights issue. `factors.py` handles SPLIT/BONUS (price+qty), the structural-break types (MERGER/DEMERGER/SCHEME_OF_ARRANGEMENT/DVR_CONVERSION), and DIVIDEND (total-return only). Add RIGHTS when a golden/backfill case needs it, computing TERP from the pre-ex close alongside the price series (the same seam `total_return_series` already uses for dividend factors). |
| M2.4 | `adjustment_factors.corporate_action_id` is written NULL by `recompute.recompute_isin`: the chain grain is per ex-date (the table PK is `(isin, ex_date)`), and two actions can share an ex-date, so a single FK back to one `corporate_actions.id` is ambiguous; `load_reconciled_actions` also does not return the row id. If an operator needs to trace a factor row to its source action(s), add the id to the loader and store the FK for single-action ex-dates (leave NULL for composed ones), or add a link table. |
| M2.4 | Pre-existing import cycle (surfaced, not caused, here): importing `dataplatform.ingest.corp_actions` *before* `dataplatform.corpactions` fails with a partially-initialized `CorporateAction` — `corpactions/__init__` eagerly imports `reconcile`, which needs `CorporateAction` at runtime (a pydantic field on `ReconciledAction`), while `ingest.corp_actions` imports `corpactions.parse_terms`/`taxonomy` at runtime. `pytest tests/unit` cannot collect ~6 modules in a fresh env because of this (true on HEAD before M2.4). M2.4's own modules were kept out of the cycle (annotation-only `CorporateAction` under `TYPE_CHECKING`); the root fix belongs to M2.2/M2.3 — e.g. move `CorporateAction` to a lower module both layers import, or defer reconcile's field resolution via `model_rebuild()`. |
| M3.1 | The legacy BSE bhavcopy (pre-08-Jul-2024, `EQ{DDMMYY}_CSV.ZIP`) has no ISIN column, so its L1 backfill needs a scrip-master-resolving source set (`bse.bhavcopy.parse_legacy` → `resolve_legacy` with `bse.scrip_master.scrip_to_isin`), not the plain UDiFF `bse_bhavcopy` set added here. `_bse_bhavcopy_request` refuses pre-cutover dates on purpose; wire the legacy L1 path when the M1.13-gated full BSE history run is greenlit, keying its `sync_state` under the same `bse_bhavcopy` name. |
| M3.1 | The UDiFF parsing logic (34-column header check, strict decimal/int/ISO-date helpers, CM/STK guards, empty-`LastPric`→0) is now duplicated in `nse/bhavcopy_udiff.py` and `bse/bhavcopy.py` — the two eras are byte-identical in format, differing only in `Src` and delivery-of-the-file (zip vs bare CSV). A shared `dataplatform.ingest.udiff` core both exchanges call would remove the copy; kept separate here to avoid an M1.5 refactor inside an M3 task. |
| M3.1 | **Fixed 2026-09-06, kept for the record.** `write_prices_raw` wrote `L1/prices_raw/date=…/part.parquet` for whichever exchange called it, so the first live BSE session would have overwritten the NSE partition for the same date — on the server, where `prices_raw` has no L0 behind it, unrecoverably. Found by sizing the BSE campaign, not by a test: `test_bse_bhavcopy` wrote BSE into an empty `tmp_path` and every acceptance held. The writer now reads the other exchange's rows back and carries them through, sorting on `(exchange, isin, symbol, series)`; `tests/unit/test_l1_prices_raw_exchanges.py` fails if either exchange clobbers the other or if the merge order changes the bytes. The lesson generalises: a "same schema as X" acceptance needs a test that writes X *and* Y into one store. |
| M3.1 | `prices_raw_quarantine` partitions are still written whole per date by `_write_quarantine`, with no exchange merge. Harmless today — only the NSE delivery join quarantines anything and BSE has no delivery feed — but the first BSE write that quarantines a row will clobber that date's NSE quarantine. Merge like `prices_raw` when it matters. |
| M3.1 | BSE rows never reach L2: `store/l2.py`'s stitch and the query layer filter `series = 'EQ'`, and a BSE row's `series` is the BSE group (`A`, `B`, `T`, `X`…). M3.2's primary-exchange selection is therefore NSE-only in practice until a BSE regular-market group set (`A`/`B`, not `T`/`Z`/`X`) is declared the EQ analogue in one place and both readers use it. |
| M3.1 | `bse.scrip_master.ingest_scrip_master` has no fetch or CLI wired to it — nothing has ever fetched the BSE list-of-scrips live, so `exchange_listing` is NSE-only (2,397 rows) and `corp_actions_backfill`'s BSE per-scrip plan is 0 units. A one-shot `python -m dataplatform.ingest.bse.scrip_master` (Active + Suspended + Delisted, per the register's survivorship note) is the missing step before any BSE CA campaign or the legacy-era bhavcopy path. |
| M3.3 | The cross-exchange rule reads `SentinelInput.exchange_closes` but no `exchange_closes_from_price_rows` builder ships yet (unlike `moves_from_price_rows`), because `PriceRow` carries no exchange — the exchange is contextual to the source/dataset. When the M3.10 daily D7 pass wires this rule, add a small builder `(rows, *, exchange, source) -> tuple[ExchangeClose, ...]` mapping each row's `isin/trade_date/close/total_traded_value/total_traded_qty`, and pair NSE and BSE rows per (isin, date) into one `SentinelInput`. |
| M2.7 | Reference A (yfinance) is unavailable for `ltim_merger_2022`: Yahoo serves no data for `LTIM.NS` (LTIMindtree) in this environment, so its golden case has a documented unavailability marker instead of a cached series (`REFERENCE_A_UNAVAILABLE` in `tests/golden/test_yfinance_reference.py`, verdict: reference B authoritative). It is the only §4.3 case with no live cross-check — a redundant surviving-entity merger (unit price factor). If Yahoo restores `LTIM.NS` coverage, re-run `uv run pytest tests/golden/test_yfinance_reference.py -m network` and delete the `REFERENCE_A_UNAVAILABLE` entry so the merger gains a real reference-A comparison. |
| M1.13 | `GapScanner`'s `LakeL1Presence` did not resolve the on-disk `prices_raw` partitions during the 10-year backfill report — it reported `L1 unverified for 2469 published pair(s)`, so `PUBLISHED` pairs are counted complete but not physically re-checked against the parquet lake (does not affect the unexplained count). Likely a lake-root/path mismatch between the scanner's `l1_partition_dir` and the writer's layout; verify the L1 partition existence probe for M1.14's audit so "we checked nothing" and "we found nothing wrong" cannot render identically. |
| M4.5 | `SimBroker` fills or rejects an order whole — no partial fills and no book-depth model. Slippage is a linear function of participation in session traded value (`SlippageModel`), which is a coarse market-impact proxy; a real thin-name order might fill partially over several sessions. Adequate for EOD paper/backtest fidelity, but if a strategy trades names where its size is a large fraction of ADV, add a partial-fill / participation-cap model (fill up to X% of the session's volume, re-stage the remainder) rather than rejecting or over-slipping. The `execute_session` seam is where that logic belongs. |
| M2.7 | Pre-existing failing test (surfaced by M4.2, not caused by it): `tests/unit/test_clock_guard.py::test_wall_clock_is_read_only_in_the_clock_module` flags `tests/golden/test_yfinance_reference.py:473` for a bare `datetime.now().astimezone()` inside a golden fixture — a wall-clock read the guard forbids outside `dataplatform.clock`. Fails on HEAD with all M4.2 work stashed. Either inject a `Clock`/freeze the timestamp in that golden helper, or add the golden-fixtures path to the clock-guard's allowlist if a captured-at timestamp is intentional there. |
| M3.8 | Resolved the pre-existing import cycle noted above (M2.4 row): `dataplatform.corpactions.reconcile` no longer imports `CorporateAction` at module top — it is `TYPE_CHECKING`-only, bound once (with `model_rebuild`) via `reconcile.bind_corporate_action()`, called at the tail of `ingest.corp_actions` after the class is defined and re-tried at reconcile import time. Both import orders now work; `pytest tests/unit` collects `test_announcements.py` (which imports `ingest.corp_actions` first). Touched M2.2/M2.3 files minimally; their suites (`test_ca_reconcile`, `test_factors`, `test_ca_taxonomy`, golden CA) stay green. |
| M4.6 | `PortfolioBook` corporate-action methods (`apply_split`/`apply_bonus`/`apply_demerger`) take primitive ratio/face-value args rather than a taxonomy `CorporateAction` object, because that model carries neither the demerger's *resulting ISIN* (established by M2.3 reconciliation on ISIN, not in the terms) nor the cost-allocation fraction the book needs. A thin adapter that resolves the resulting ISIN via the identity/reconciliation layer and the cost fraction via relative fair values on the ex-date, then dispatches a stored `CorporateAction` into these methods, would let the daily/backtest loop replay CA events straight from the store without the caller unpacking terms by hand. Belongs where the backtest driver (M4.7+) wires the query layer to the book. |
| M5.2 | The cross-case concentration rail reuses `RiskRails.max_position_pct` as the household ceiling — §5.2 defines no separate ratified number for household concentration, so a single-case position cap and the whole-household cap are the same figure. If the owner wants the household bound looser or tighter than a single case's (e.g. 20% household vs 15% per-case), add a `max_household_pct` to `RiskRails` (with a validator that it is >= `max_position_pct`) and point `_cross_case_breach` at it; the rail and its test are already isolated for that swap. |
| M5.2 | A8 consumes `analyst.cases.RiskRails` and `HouseholdExposure` is assembled by the caller from `analyst.cases.CrossCaseExposure` (quantity-based) plus current prices. No builder ships for that assembly yet — the daily loop (M5.11/M5.13) must turn each `CrossCaseExposure` + a price map into a `HouseholdExposure` per instrument before calling `check_order(..., household=...)`. Add `HouseholdExposure.from_cross_case(exposure, prices)` when that loop lands, so the value-based view is derived in one place rather than at each call site. |
| M4.7 | The SIP allocator reports `tracking_drift` on the *invested* book (holdings shape vs model), while its greedy optimiser minimises drift denominated over *total capital* (idle cash counts as underweight). Cash drag is captured separately as `residual_cash`. When M4.8/A9 journals SIP instalments, decide which drift figure is the journaled tracking number (invested-shape vs capital-including-cash), or journal both; the two answer different questions and downstream reporting should not conflate them. Also: a name whose single-share price exceeds its model weight × current total capital is structurally unbuyable until the book grows (correct behaviour, but it means small-weight expensive names stay permanently underweight in a small book) — the rotation/accumulation layer may want an explicit policy for that case. |
| M5.7 | Interview exclusions match by ISIN or case-insensitive substring against a candidate's name and value-chain stage (`analyst.interview.proposal._apply_exclusions`). A short token ("AI") can over-match, and there is no sector taxonomy behind a "no fossil fuels" exclusion — it is pure string containment over the theme map's text. When D2/sector classification is wired into the universe, promote exclusions to structured predicates (by sector code, by ISIN, by explicit keyword) so the human's intent is matched on data, not on substrings. The `_apply_exclusions` seam is where that swap belongs. |
| M5.7 | The default cash-parking instrument (`DEFAULT_PARKING_ISIN`/`DEFAULT_PARKING_SYMBOL` = LIQUIDBEES) is a module constant, not resolved through the identity master (D2). `build_proposal`/`recommend_cash_policy` accept an override, but there is no check that the parking ISIN is a live, tradeable liquid ETF as-of the proposal date. When the daily loop funds a case, resolve the parking instrument via the identity/universe layer and validate it is tradeable on `as_of`, rather than trusting a checked-in constant. |
| M5.12 | A broker-*rejected* staged order is not transitioned in the order journal — `StagingCoordinator.execute` skips non-COMPLETE fills, leaving the `order_` row in `STAGED` forever (`OrderJournal` has no `record_rejected`, and the `order_` schema has a `REJECTED` state that never gets written). Harmless today (a past-session STAGED row is never re-fetched by `due_for`, which filters on `staged_for_date`, and recon compares positions/cash not order states), but M5.13's multi-session paper run will accumulate stale STAGED rows and a genuine broker rejection should be a first-class, journaled event (like `RAIL_BLOCKED`). Add `record_rejected(order, reason)` to the `OrderJournal` seam and post REJECTED fills to it in `execute`. |
| M5.12 | `StagingCoordinator.execute` drives fills by calling `SimBroker.execute_session`, which is not on the `Broker` protocol (real fills come from the exchange, not a caller-stepped session) — so the coordinator is typed to `SimBroker` concretely, unlike `Reconciler` which depends only on the `Broker` protocol. At M8 the KiteBroker path needs a different execution driver (poll `positions()`/order status rather than step a session). Introduce an execution-driver seam (paper = step SimBroker; real = reconcile against exchange-reported fills) so `StagingCoordinator` can be broker-agnostic like the rest of the staging/recon surface. |
| M7.3 | The XBRL fixtures under `tests/fixtures/xbrl/filing_v1/` are representative documents modelled on the in-bse-fin 2020-03-31 taxonomy shape (built offline — the test suite never touches the network, B8), not byte-frozen copies of the real NSE filings the C.1 sweep sampled. The register rows (`nse_financial_results_index`, `nse_xbrl_filing`) are already `VERIFIED` with real sample sha256/size. When a real fetch runs, freeze one genuine index response and its linked XBRL document under the same fixture path and re-point the parser test at them, so the parser is exercised against real-world quirks (comparative-context proliferation, company-defined segment members, unit variations) the representative fixtures approximate. |
| M7.3 | No end-to-end ingest *runner* ships for the PIT filings path — `discovery.parse_index` and `parser.parse` are offline parsers, and the store is `pit_fundamentals.write_pit`, but nothing yet drives fetch(index)→L0→parse→per-filing fetch(xbrl)→L0→parse→write_pit through the `Fetcher`/`SyncTracker` with resume/checkpoint (cf. `shareholding.ingest_snapshot`). A full-universe pull is a B1 bulk campaign (`>200` requests to one source → NEEDS_GO). When the daily EOD job (M1.10) wires fundamentals, add the runner: it fetches the day's `corporates-financial-results` index, then each entry's XBRL, keyed idempotently in L0 by the seq-number-bearing filename, and threads each entry's `filing_date` into `parser.parse`. |
| M5.10 | A7's `CashManager` reads `cash_policy.deploy_within_sessions` (the ratified forced-deploy deadline, §5.2 policy 6) but does not enforce it — `DeploymentQueue.oldest` exposes each tranche's arrival date, yet nothing measures elapsed *trading sessions* against the limit (that needs the session calendar the daily loop owns). When M5.11's T0 monitor lands, have it compute sessions-elapsed for `queue.oldest` and force a deployment (or escalate) once the deadline is reached, so parked cash cannot sit past its ratified window. The `oldest`/`arrived` seam is where that check reads from. Also: `deploy` draws only from queued cash and produces the target BUY; the mechanical un-park (selling the ETF to raise the rupees) is the inverse of `park` and should be staged by the same daily loop, one order per leg. |
| M5.8 | The ratification UX (`analyst.cases.web`) lets a reviewer edit the policy-driving interview answers (SIP, horizon, risk appetite, concentration, benchmarks, top-up) but deliberately not `exclusions` — removing a holding changes the universe and the thesis set together, so it needs to filter the per-holding theses down to the survivors as well as re-run `build_proposal`. `EDITABLE_ANSWER_FIELDS` is the seam; add exclusion editing there once the page has a per-holding "drop" control and the thesis map is filtered to the surviving universe (mirror `proposal._apply_exclusions`, or promote it to public). |
| M5.8 | `web.InMemoryProposalStore` is the only `ProposalStore` shipped — proposals under review and their `RatificationRecord`s live in process memory, so a restart loses an in-flight review and the artifact of an approval. The `ProposalStore` interface is where a durable backing belongs: stage the draft on `case_.config` (like A1's `pending_policy`) and, on approval, drive `CaseService.propose`/`ratify` so the ratification lands in `policy_set` and the case moves `PROPOSAL→RATIFIED` in one journaled transaction. That wiring is M5.13's end-to-end path; until then the UX produces the record but does not persist it to the case store. |
| M5.13 | The daily analyst session that composes the M5 parts (interlock + A5 T0, A7 park/deploy, A8 rails, A9 journal, X1 staging over the injected `Broker`) lives in the paper-run test (`tests/integration/test_paper_run_10_sessions.py::_make_paper_session`), not in a product module — M5.13's scope was to *prove* the parts compose, and no standing daily-loop entrypoint is in M5. When the live daily loop is built (it wires the same pieces plus the real `StatusApiGate`, `PostgresOrderJournal`, `HouseholdExposure.from_cross_case`, and the deploy-deadline check the M5.10/M5.2 rows note), promote this composition to `analyst/` with the test's session function as its reference shape, so the production loop and the paper run run identical code (invariant #5 in spirit for the loop itself, not just the broker). |
| M5.15 | The auth interlock ships as a composable component (`analyst.monitor.interlock.AuthInterlock` + `execution.session.BrokerSessionGate`), but no single daily-loop module yet *calls* `guard()` before `StagingCoordinator.stage()` and re-stages the returned `deferred` decisions on the next valid session — the wiring is demonstrated in `tests/unit/test_auth_interlock.py::run_daily_loop`. When M5.13's paper run / M5.14 assemble the real daily loop (alongside the data-red `T0Monitor` interlock), inject one long-lived `AuthInterlock` (so once-per-streak dedup holds across sessions), run it next to `is_green()`, and feed each day's `AuthInterlockResult.deferred` back into the following authenticated session's decision set. |
| M6.2 | The news→holding matcher (`analyst.monitor.matcher.match_links`) produces `NewsMatch`es with an `IMMEDIATE`/`ROUTINE` urgency, but nothing yet composes it into the T0 daily sweep — `T0Monitor` still only runs `check_announcements` over exchange announcements (which arrive ISIN-keyed). When the daily loop (M6.3/M6.4 evidence path) wires the GDELT/RSS feed in, build the `NameResolver` from the day's held ISINs once, run `link_all` over the day's `NewsRow`s, then `match_links` against each holding's T0-tier `BreakConditionWatch`es (its thesis conditions + `default_integrity_watch()`), and enqueue the `to_t0_flag(case_id)` output on the same `EscalationQueue` — an `IMMEDIATE` match must escalate the same session (§5.3 BC3 / §5.6). `NewsMatch.to_t0_flag` is the seam. |
| M6.2 | `NameResolver.build` derives every exchange symbol an ISIN ever traded under (all `windows_for` windows, any date), not the symbol valid on the news item's date — a recycled ticker could in principle mis-link a dated headline to a held name that used that symbol in a different era. The cross-ISIN ambiguity guard catches same-era collisions between two *held* names, but not a stale symbol reused by a name we do not hold. When PIT discipline matters for news (M6.3 bundle build), scope the derived tickers to windows covering the item's `ts.date()` via `SymbolWindow.covers`, mirroring `IdentityMaster.resolve`'s as-of rule. |
| M4.10 | The naive momentum backtest reads L1 raw closes directly (adjusted == raw only because this lake has no corporate actions) rather than L2 adjusted cross-sections through `QueryService`. When L2 `prices_adjusted` is materialized (M2.5) with real factors, re-point `_L1MomentumData`/`_L1Market` at `QueryService.cross_section`/`adjusted_series` so momentum ranks on genuinely back-adjusted series and dual-listing dedup applies — the seam is `_L1Reader`. Likewise the PIT universe is built from L1 observed listing windows, not the identity master; swap `_L1Reader.listing_windows` for `store_listing_calendar` + index membership once D2 is loaded. |
| M4.10 | The backtest benchmark is a broad-market TRI **proxy computed from L1** (`_build_benchmark_tri`), standing in for the licensed NSE NIFTY-TRI series which is not in the store. When the real TRI is ingested (`dataplatform.ingest.indices`), feed the published `TriSeries` into `run_naive_momentum` instead of the proxy — the `compare_to_benchmarks` call is unchanged. Also add a genuine theme-proxy benchmark (currently theme == broad market) once a sector/theme index is available. |
| M4.10 | Buys deploy only currently-free cash (never same-session sale proceeds), so cash freed by a rebalance's sells is redeployed one rebalance later — a deliberate simplification to keep the run rejection-free without modelling intraday cash timing. If a later strategy needs same-session recycling, add an execution-driver that fills staged sells before buys within one `execute_session` and sizes buys against expected post-sell cash with a costed buffer. |
| M4.11 | CI (`.github/workflows/ci.yml`) runs the fixtures-only path only — it provisions no Postgres service, so database-backed `integration`-marker tests skip there and are covered only behind `make up` locally. When a hermetic Postgres is worth the CI minutes, add a `services: postgres` block, run `make migrate` against it, and drop a `DATABASE_URL` into the env so the skipped integration tests execute; keep the offline job as the fast default gate. |
| M4.11 | The PIT-leak harness (`tests/integration/test_pit_leak.py`) is self-contained: its auditor patches `PitContext.admit` and its in-memory store/market/policies live in the test module. The instrumentation is generic (any replay, any record type with an independent knowable-date reader) and would let a future backtest emit a leak certificate ("N reads, 0 leaked") for a real strategy run. If a second caller needs it, promote `PitAuditor`/`Access`/`Leak` to a small `backtest/pit_audit.py` and import it here, rather than copying the patch logic. |
| M8.1 | `KiteBroker.ledger()` reads a placeholder `/ledger` path — Kite Connect's core REST has no order-independent cash ledger, so the real cash statement comes from Zerodha's Console reports API, whose exact endpoint/auth differ from the trading API and are unavailable without a live login (B4). When M8.3's human-gated live sessions run, confirm the real Console ledger URL + payload against a captured response, refreeze `tests/fixtures/kite/ledger.json` from it, and point `_PATH_LEDGER` at it. Likewise `LiveKiteTransport` (httpx) is unexercised by the offline suite (no token, no network) — add a manual/integration smoke test at M8.3 that runs one read (profile/margins) against the live endpoint before any order. |
| M6.4 | `T1Reviewer` escalates a malformed answer or an out-of-policy action by journalling an `ESCALATE` (actor T1, decision ESCALATE) — it does not enqueue anything for a human to pick up, unlike T0's `EscalationQueue`. A BROKEN verdict with an in-policy exit sets `T1Result.exit_triggered` and journals the exit directive in the entry payload, but no A7 consumer reads it yet. When A7 (`analyst/monitor`→exits) and any human-review inbox are built (M6.5/M6.7/M7.4), give `T1Reviewer` a queue/handoff seam so an `exit_triggered` result drives the A7 exit path and an `ESCALATED` result reaches the human, rather than living only as a journal line a reader must poll. The `T1Result` fields (`exit_triggered`, `proposed_action_kind`, `exit_strategy`, `rejection`) are the seam. |
| M1.10 | The daily EOD job publishes an archive bundle for the *target* session only (`EodPipeline._publish_archive(target)`); a session that failed and self-heals on a *later* run reaches PUBLISHED but is never archived, because the later run archives its own target and the healed straggler is not re-visited once PUBLISHED. Harmless for a same-day heal but a real gap for a cross-day one (M1 gate box 4, "archives downloadable", would miss the healed day). When the archive path is revisited, have the run publish a bundle for every session it moved to PUBLISHED this run (target + healed), not just the target — the `report.healed`/`_publish_archive` pair is the seam. |
| M4.10 | **(FIXED)** Boundary bug: a backtest window ending on the last session on disk crashed mid-run with `NoReferenceBarError` from `SimBroker.place`, because T+1 execution stages a fill for the *next* session and a rebalance landing on the final bar had none — the `_L1Market` "market calendar outruns the replay window" invariant did not hold at the data edge. `run_naive_momentum` now calls `_reserve_fill_headroom(sessions, calendar)`, which reserves the final on-disk session as a fill target (never replayed) when the window reaches it, or refuses loudly if nothing is left to replay. Verified: `--to 2026-09-01` now runs and yields the byte-identical digest to `--to 2026-08-31`. Unit-tested in `test_naive_momentum.py::test_fill_headroom_is_reserved_at_the_data_edge`. Note carried forward: a live daily loop hitting "rebalance due, next bar not yet published" should *defer* the rebalance to the next session, not treat the edge as terminal — that policy belongs to whoever wires the production loop (M5.13). |
| owner | **OpenRouter gateway provider (X3), to reach ChatGPT/Gemini/etc. without integrating each LLM in the core.** Owner wants multiple models behind one key/bill (drivers: cost + simpler billing), not N provider adapters. The `LLM` protocol + `build_llm` switch already make this a single new implementation: add `OPENROUTER` to `LlmProvider`, an `OpenRouterLLM` (OpenAI-chat shape) sibling to `analyst/llm/anthropic.py`, and make the tier models config (`DEFAULT_MODEL`/`TRIAGE_MODEL` are hard constants today) so "GPT for T2, Claude for triage" is a config edit. Cost model: record OpenRouter's returned per-generation cost (more accurate than a static card for billing) and add OpenRouter entries to `model_prices.yaml`; map the 4-rate `Usage` to what each route reports, book 0 cache tokens where a route does not cache (do not overstate as cached). Keep direct `AnthropicLLM` as the production path for Claude tiers (preserves prompt-cache economics on whole-case T2 reads and keeps holdings/theses off an intermediary); OpenRouter is added *alongside* for breadth (serves M6.8's multi-model quality experiment). Caveat to carry: OpenRouter sits in the data path and sees every prompt — keep the option to pin sensitive tiers to a no-log route. Build later, when needed. |

- QueryService.cross_section raises a raw DuckDB BinderException over an empty L2 (no partitions materialized) instead of a typed QueryError / empty result; the backtest's default adjusted run needs L2 materialized first. Harmless for the fixture path but a rough edge for a cold store. [spotted in M9.2]
- M9.3's as-of index-membership screen (`UniverseParameters.index_slug`, default `nifty500`) is inert on the real store because `index_constituents` is empty — M3.9's snapshot history accrues one month at a time from day one and no ten-year backfill of prior months exists to fetch (AGENTIC_CONTEXT §4.1). So the ten-year universe run is narrowed by the liquidity floor alone; the membership intersection is only exercised on the integration fixture. Once monthly constituent snapshots have accumulated (or a licensed history is sourced), the same screen constrains the live universe with no code change. Also: the universe thresholds (`index_slug`, `median_turnover_floor`, `liquidity_lookback_days`) are a-priori constants with no CLI override on `--universe-report`; expose them if a sensitivity sweep is wanted. [spotted in M9.3]

- M9.5's `--v2-report` runs the six configs (naive + four increments + all-on) as six independent `run_momentum_v2` calls, each building its own `_L1Reader`, `_RegimeSource` (a full-calendar daily proxy-index level series) and monthly volatility maps from scratch — the regime levels and vol maps are identical across configs and are recomputed six times. A shared precompute (one reader/regime/signal cache passed to all six) would roughly halve the ~9-minute report runtime. Also: the regime overlay reads a broad-market **L1 proxy** index and volatility is struck on 12 *monthly* returns (cheap, ample for sizing) — swap in the licensed index level and a daily-return vol if precision is wanted later. [spotted in M9.5]
| M9.1 | **Live CA fetch attempt (2026-09-03) surfaced 3 blockers to the adjusted-price payoff — the runner ran (11 NSE date-chunks, not the assumed thousands: `prices_raw` holds no BSE data and the identity master is NSE-only, so 0 BSE per-scrip units), but landed 0 corporate actions.** (a) **Identity master was empty at the time of that attempt** — `security_master`/`symbol_history`/`exchange_listing` = 0 rows, so every symbol→ISIN resolution failed and all 5,258 parsed actions landed `unresolved`, even though the NSE feed carries a native ISIN (the runner resolves through D2 by design, invariant #2). **CORRECTED 2026-09-04: D2 is now populated** — `security_master` 2,397, `symbol_history` 2,886, `exchange_listing` 2,397 (verified by query). This clause is kept only as the record of why that run landed nothing; **do not cite it as the current state.** It has already misled one later analysis into treating D2 as a live blocker on `nse_sec_bhavdata_full`, `nse_bulk_deals`/`nse_block_deals`, `bse_bhavcopy_legacy` and F&O *stock* aggregates — those are blocked on their **fetch campaigns** (owner GO under B1), not on identity. What remains thin is *historical* symbol coverage for resolving decade-old (symbol, ex-date) pairs, which is the M1.7 row above, not an empty master. (b) **`RightsTerms.premium_inr` and `DividendTerms.amount_inr` use `Field(gt=0)`, but a rights issue at par has premium 0** (legitimate) — 6 of 11 date-chunks failed with a `ValidationError`. `premium_inr` should be `ge=0`. (c) **A single bad row fails the whole date-chunk unit**: a term that parses to numbers then fails pydantic model validation escapes the parser's per-row try/except and bubbles up, failing a year's fetch — it should be routed to the M2.1 queue like other unparseable rows, so one par-rights issue can't take down a decade-chunk. Net: a trustworthy 10-year *adjusted* backtest needs the historical identity master built out (a known-thin component — M1.7 rows above: one snapshot can't resolve 10y of historical (symbol, ex-date) pairs) plus (b)/(c) fixed, then a CA re-run + factor recompute + L2 materialize + backtest re-run. |
| M9.1 | **The adjustment pipeline is architecturally gated on cross-exchange reconciliation — NSE-only data yields 0 factors even after CAs land.** After fixing identity + the term validators, the live CA backfill persisted 2487 real NSE actions, but `finalize_reconcile_and_recompute` produced 0 factor rows: `reconcile.py` is explicit that "one corporate action both exchanges agree on [is] the only kind the factor chain may consume" (SINGLE_SOURCE is a `quality_flag` a human must adjudicate, never a factor). With no BSE CA data, every NSE action is single-source → flagged → no factor → L2 adjusted stays == raw. Delivering a split-adjusted backtest therefore needs EITHER (a) BSE CA ingestion so actions reconcile (needs BSE identity/scrips, also not ingested), OR (b) an owner-ratified design change letting a single-source action from a single-listed security become a factor with the SINGLE_SOURCE flag as a caveat — a change to the "only agreed actions reach the factor chain" invariant (M2.3), reserved to the owner. This is the real, final blocker to the M9 adjusted-price payoff. |
| M9.1 | **The CA backfill runner is not cleanly re-runnable against the live NSE feed.** A re-run after a prior attempt failed 9 of 11 date-chunks with `L0ImmutabilityError`: the NSE `corporates-corporateActions` endpoint returns different bytes each fetch (it includes live/near-term actions), and L0 is write-once (invariant #1), so re-fetching collides with the prior run's stored payload. Resume correctly skips PUBLISHED units, but a unit that needs re-processing (e.g. after an identity/parser fix) cannot re-fetch. The runner should re-parse from the already-stored L0 on a forced re-process (FETCHED→VALIDATED→… without re-hitting the network), or the CA L0 for the window must be cleared before a clean re-fetch. |
| M9.2 | **Price-adjustment (splits/bonuses/rights) is coupled to the total-return (dividend) computation in `build_adjusted_bars`, so a bad dividend blocks a name's split adjustment.** `total_return_series` raises on a dividend stated as a percentage of face value ("resolve it to rupees" — needs the identity master's face value, which this function does not fetch) or one with no prior close; that raise fails the *whole* L2 materialize for the ISIN, losing its price adjustment even though dividends never affect the price factor. ~78 of 252 factored names (incl. Infosys INE009A01021) fell back to raw for this reason in the M9.2 run. Fix: (a) decouple — build the price-adjusted series even when the total-return leg can't be computed; and (b) resolve a percent dividend to rupees via the D2 face value. Both are M2.4/M2.5 changes. |
| M9.2 | **`materialize_isins` has no per-ISIN error isolation** — one ISIN raising (e.g. the duplicate-price/dividend cases above) aborts the whole batch via the generator. It should collect per-ISIN failures and continue (the ad-hoc M9.2 materialize did this in a loop), returning both the written reports and the failures, so a bad name never blocks the rest of a bulk rebuild. |
| M9.1 | **Historical identity resolution is thin: 5,096 of the CA feed's actions went `unresolved` and 8,766 `queued`.** The identity master is a single 2026-08-08 `EQUITY_L.csv` snapshot, so a name whose symbol changed or which delisted before then does not resolve as-of its historical ex-date — recent CAs resolve, a decade of older ones largely do not. A genuinely trustworthy 10-year adjusted history needs accumulated weekly EQUITY_L snapshots (or BSE listing history) behind `IdentityMaster.resolve` (see the M1.7 rows above). Until then the adjusted backtest is correct where it adjusts but under-covers older corporate actions. |
| M10.5 | **Balance-sheet fundamentals for quality signals (ROE/ROCE/P-B/debt).** The M7 XBRL parser extracts only P&L concepts (revenue, income, expenses, PBT, PAT, EPS), so M10's fundamentals signals are limited to value (P/E) and growth/margin. ROE, ROCE, book value (P/B) and leverage need balance-sheet line items — either extend `xbrl/models.py` CONCEPTS + parser to the balance-sheet filing (equity, total assets, borrowings) or add the Screener restated source (currently BLOCKED_CREDENTIAL, and monitoring-only per invariant #8). A prerequisite for a real quality factor, not just value/growth. |
| M7.3 | **Balance-sheet and segment-result facts are one parser change away now.** The rebuilt parser resolves contexts by column and dimension axis, and the captured filings show `SegmentProfitLossBeforeTaxAndFinanceCosts` (on `ReportableSegmentsFinanceCostsAxis`), `SegmentAssets` and `SegmentLiabilities` sitting alongside the `SegmentRevenue` this task extracts — same columns, same axes, already parsed into `_ContextShape`. Note one wart for whoever does it: for liabilities the segment *name* is in the duration context (`OneReportableSegmentLiabilities01D`) while the *value* is in the instant one (`…01I`), so name and value must be joined across the D/I pair rather than read from one context. This is the cheap half of the M10.5 quality-factor row below (ROE/ROCE/P-B still need equity and total assets, which the results filing does not carry at all). |
| M10.4 | **The Q4 quarter of an annual-only filing is recoverable but not recovered.** A minority of pre-2023 documents (3 of 25 in a live FY2019-20 sample) hold only the cumulative column, yet the index lists them under a fourth-quarter entry as well. The parser refuses the quarter, because its start is stated nowhere in the document and storing twelve months under three would be silent (`ops/gates/M10-fundamentals-backfill-live.md` §6). The figure *is* derivable as (annual − nine-month cumulative) once both filings are in the store, but that is a computed value and belongs in a derived layer with its own provenance, not in a PIT store whose contract is fidelity to what the filing said. Worth building if a quarterly fundamentals signal turns out to need the Q4 of that era; the annual figure for those companies lands normally in the meantime. |
| M10.4 | **Index history goes back 10 years, so the "forward only" framing in M7.3's spec is too pessimistic for the discovery half.** The `corporates-financial-results` feed filters on *broadcast* date and returns every reporting period in the window: 135,197 announcements over FY2016-17..FY2026-27, of which 101,446 have an XBRL document (documents start around FY2018-19; the first two years are 100% placeholder). M7.3's spec says "history only accumulates forward from now — do not backfill PIT fundamentals from a restated source", which remains right about *restated* sources but reads as if the filings path had no history either. It has ~7 years of it, and it is genuine PIT (each entry carries its own broadcast date). Worth amending the spec text so a future reader does not under-scope the backfill. |
| M10.4 | **DONE 2026-09-03 — L0 payload reuse implemented (`_ref_for`).** Kept for the record: one document serves 1.26 index entries, so fetching per entry re-requested bytes L0 already held 20,849 times (20.6% of the campaign, ~17 h of wall-clock). `_process_filing` now reads the existing L0 ref when there is one and fetches only when there is not; the parse still runs per entry, since the entry selects the results column. |
| M10.4 | **No scheduled forward-sync job for PIT fundamentals.** M10.4 built the runner; nothing runs it. The scheduler registry holds `eod_pipeline` and `constituents_snapshot` only, so new filings are picked up solely by an operator re-running the CLI with `--to` moved forward (`ops/runbooks/fundamentals_backfill.md`, "Picking up new filings"). This is the M7.3 backlog row's "when the daily EOD job wires fundamentals, add the runner", now half-done. A job is small — a `Job` in `dataplatform/scheduler/registry.py` calling the runner over a trailing window (say the last 30 days of broadcast dates, which re-reads a couple of index chunks and fetches only what is new) — but three things are the owner's call: whether it lives in the daily EOD pipeline or its own cron, how wide the trailing window is, and whether it may run before the 10-year campaign has been given its B1 go. |
| M7.3 | **DONE 2026-09-05 (`9f26a83`) — `taxonomy` is persisted, and `derived` alongside it.** Kept for the record: `FundamentalFact` and `_L1_SCHEMA` now carry `taxonomy`, so a consumer can see from the row that a bank's `total_expenses` is `ExpenditureExcludingProvisionsAndContingencies` and excludes provisions — the heuristic workaround this row described (inferring bank-like filings from `total_income - total_expenses != profit_before_tax`) is no longer needed. Done in the same migration and the same re-derivation as the balance-sheet concepts, exactly as this row and the M10.5 row below both recommended, after the campaign finished. Zero network requests. |
| M10.4 | **156 filings, not 21 — measured 2026-09-06 once the gap report could run.** The shape of the row is unchanged and so is the fix (gap-plan Action 3); the number was taken from one campaign's diagnosis and never revisited. `sync_state` holds 156 FAILED filings whose document states a symbol D2 does not accept on the filing date, against 1,463 blocked by the integrated feed's period guess and 774 whose document was never fetched into L0 at all. Original text: **21 filings blocked by D2 symbol history, not by the parser — 2 companies, both genuine renames.** |
| M10.5 | **DONE 2026-09-05 (`9f26a83`, `b2e3dbb`) — the balance-sheet concepts are extracted.** Kept for the record: this row corrected an earlier claim that balance-sheet items needed a new source; they were already in L0 and the whitelist simply stopped at eight P&L items. Shipped as a parser change at zero fetch cost. Measured outcome over the re-derived store: the capital pair at 100% of filings, so `shares_outstanding` (and market cap) on ~95% — the other ~5% are refused by a guard against filers who state paid-up capital out by a clean power of ten; `shareholders_equity_excl_revaluation` on ~19%, being the annual filings that actually fill the reserves tag (29.5% of those that state it state 0.00, which is an unfilled mandatory field, not a zero); `debt_equity_ratio` on ~28%; and bank asset quality — gross/net NPA as amounts and percentages, CET1, return on assets — at 100% of bank filings, which this row did not anticipate. Still genuinely absent, unchanged: no cash-flow statement, ROCE only approximable, ROE annual rather than rolling. Segment assets/liabilities deliberately deferred as one piece of work (`SegmentLiabilities` splits name and value across a D/I context pair the segment path cannot yet express). Detail in `ops/gates/M10-data-gap-plan.md`. |
| M1.6 | **DONE — the delivery backfill ran (`9456c40`…`0ae0fb7`).** Kept for the record, and corrected here because the row below said the opposite for weeks after it stopped being true: `deliv_qty`/`deliv_pct` are populated on 59.1% of `prices_raw` rows and 78.6% of EQ rows, over all 2,471 sessions. The ~41% without are not a gap — the trade-to-trade and debt series (`BE`, `BZ`, `GS`, `SM`…) publish `-`, which the parser models as `None` and never as 0. What *is* a gap is the EQ side: 1,077 of 3,635 EQ symbols have never once received a delivery figure, because the delivery file has no ISIN and the identity master cannot resolve them (gap-plan Action 3, and 593 of them are fund/ETF ISINs Action 3 structurally cannot reach — see the 2026-09-06 audit N5). Original text: **The delivery file was never fetched, and that is the only thing missing.** |
| owner | **S3-backed L0 (raw → object storage, local = derived only): PARKED 2026-09-03.** Owner's instinct is right architecturally (immutable L0 in S3 as the recomputation source of truth), but the numbers don't force it yet: the full 10-year raw dataset at EOD scope is only ~20–50 GB (drivers: F&O EOD ~10–20 GB, fundamentals XBRL ~5–10 GB, cash prices ~0.5 GB, everything else ~1–3 GB), and derived L1/L2 is ~3–6 GB — all comfortably inside the 300 GB local budget. Revisit if scope grows to intraday or full option-chain data (which pushes raw into the 100s of GB), or when a second machine/durability requirement appears. When built: an S3 backend for `L0Store` (write-through on ingest, read-on-recompute, preserving invariant #1 immutability), mirroring the local `L0/<source>/…` tree; creds via `.env`/env (never in-repo), and fold L0 into the backup target (the `setup` object-storage row above). |
| M7.3 | **RESOLVED 2026-09-03 — rebuilt against real filings; see `ops/gates/M7-xbrl-rebuild.md`.** Kept for the record: this row documented M7's XBRL path as a false-DONE, gate-passed against fabricated fixtures and unable to ingest a single real NSE filing. All four defects it named are fixed, plus two more the rebuild surfaced (the feed's hyphenated `Un-Audited` read as "did not say"; `read_latest` collapsed a Q4 and a full year ending the same day). The root cause was not the four bugs but the format model: real NSE results XBRL is a column-by-column transcription of the published results table, and the index entry selects the column. Original text: **M7's XBRL fundamentals path is a false-DONE: built and gate-passed against fabricated fixtures, it cannot ingest a single real NSE filing. Needs a real rewrite, not a patch.** Confirmed 2026-09-03 by running the M10.4 runner against the live feed + a real filing (VSTTILLERS, INE764D01017, xbrl INDAS_121277_1705282_30072026051753.xml). Four structural mismatches, each hidden by the hand-built fixtures, surfaced in sequence: (1) **discovery nature** — the real `consolidated` field is `Consolidated`(1630)/`Non-Consolidated`(2186), never the fixtures' `Standalone`, so `_nature` rejects every real entry; fix: map `Non-Consolidated`→STANDALONE. (2) **entity identity** — real XBRL identifies the entity by NSE **symbol** (`scheme="http://www.nseindia.com/NSESymbol"`, e.g. `VSTTILLERS`) and carries **no ISIN element**; the parser assumes entity==ISIN and validates it against ISIN_PATTERN, so every filing raises. Fix: take ISIN from the index entry (`FilingIndexEntry.isin`, already captured), cross-check the XBRL **symbol** against `FilingIndexEntry.symbol`, never read ISIN from the document (invariant #2 via the index / D2). (3) **multi-period** — real filings report each concept for several periods (quarter + YTD + prior-year); filtering by `period_end` alone still admits the YTD figure (same end, earlier start) → duplicate-concept error. Fix: match the full `(period_start, period_end)`. (4) **duplicate same-period contexts** — even matching the full period, a real filing had two contexts (`OneD`, `FourD`) with identical period and no distinguishing dimension both carrying `RevenueFromOperations`; and its period (Dec-2024) did not match its filing_date (Jul-2026). The parser's one-value-per-(concept,segment) context model does not fit real NSE INDAS XBRL. **This is a proper rebuild**: study several real filings' dimensional/period structure, re-freeze fixtures from *captured* real documents (not hand-built), rewrite the parser's context/period/nature selection, and update M7.3 + M7.4 (BC1 segment) + M7.5 (gate) + M10.4/M10.5/M10.6 which all sit on it. The partial fixes for (1)-(3) were prototyped and reverted (they broke the fabricated-fixture tests without completing real ingest); they are recorded here for the rebuild. Until then M7 fundamentals hold no real data and the M7 gate's PASS is against fabricated fixtures only. |
| M10.4 | **DONE 2026-09-05 — the re-derivation's write path was quadratic; it is now batched.** Kept for the record, because the shape recurs wherever a partition is rewritten per record. `write_pit` reads, merges and rewrites a whole `filing_date` partition per filing, so a results-season partition — 762 filings on 2019-08-14, against a median of 19 over 1,775 partitions — was rewritten 762 times, and re-deriving the store from L0 spent about ninety minutes almost entirely in that path. `PitFundamentalsBatch` buffers each partition's facts and writes it once per index chunk; measured over 10,132 real filings the write path went from 410s to 3.4s (120x) with all 199 partitions byte-identical to the per-filing output. The part worth remembering is the checkpoint, not the speed: buffering the writes without moving the checkpoint would have let a committed `PUBLISHED` row claim a filing whose facts were still in memory, which no later run could detect, because `PUBLISHED` is the one state the resume check skips. So a batched run writes nothing to `sync_state` until the flush has every partition on disk, and it batches only under `--rebuild-from-l0`, where a lost buffer costs local reads and parses rather than NSE requests. Two things the deferral needed and the per-filing path did not: the buffer has to answer the resume question itself (the feed repeats an announcement verbatim — 15 `(filing_id, filing_date)` keys repeat inside one real chunk), and an illegal `sync_state` transition has to surface at buffer time, where it fails one filing, rather than at the flush, where it would abort the batch. |
| audit 2026-09-06 | **Re-derive `pit_fundamentals` from L0** (`fundamentals_backfill --rebuild-from-l0`, zero network, ~12 min) to purge the 65 Dhunseri Tea filings stored under Dhunseri Ventures (INE477B01010) before the as-of identity check landed (`39da7c7`). Until then `compute_metrics` still sees them for that one ISIN. Do not run alongside another lake writer. |
| audit 2026-09-06 | `SimBroker` fills are quantised to the paisa; NSE's tick is ₹0.05 above ₹250 since 2024-06 and ₹0.01 below. A tick-regime table in the fill policy would make fills exactly exchange-printable. |
| audit 2026-09-06 | The M9.2 delta report renderer (`render_delta_report`) still says `corporate_actions`/`adjustment_factors` are empty and the delta is zero by construction; the lake now holds 9,573 actions and 326 factor rows for 252 ISINs, so `--delta-report` would show a real adjusted-vs-raw difference and the prose must become conditional before it is regenerated. |
| audit 2026-09-06 | The M10.3 and M9.5 reports start at the lake's first session (2016-09-02), so their first year is necessarily in cash while the benchmark is fully invested from day one — a window property, not a code defect (regenerating M10.3 after the look-back fix reproduced it). A fairer comparison would start both the strategy and the benchmark at the first rebalance that has a signal (2017-09), or fund the benchmark with the same cash schedule the strategy actually deploys. |
| audit 2026-09-06 | XBRL filings stated at the wrong unit scale (PFC Dec-2023 100x small, PTC FY18 100x large, KAYNES paid-up 1e6x) are excluded at derivation (M10.5 scale guard). A parser-level detector would need cross-filing context the parser does not have; consider a D7 quality rule that flags a filing whose paid-up capital jumps 10^k against the ISIN's previous filing. |
| audit 2026-09-06 | Screener sanity: Screener re-bases historical EPS for later bonuses/splits (6.8% of quarter pairs differ by a clean CA ratio). A CA-adjusted EPS view (`eps_basic x cum_qty_factor`) from the D3 factor chain would let the monitoring surface compare like with like. |
| audit 2026-09-06 | Portfolio-level overlays not built tonight: a volatility target (scale gross exposure to a stated annualised vol) and a drawdown de-risking rule (halve exposure past a stated peak-to-trough fall until the regime turns) — both a-priori, both cheap to add as v2 toggles once the redeploy row has been read. |
| audit 2026-09-06 | **The PIT fundamentals store ends in Feb-2025.** Filings per year: 2024: 13,168 · 2025: 2,909 (all before 2025-02-14 for the 90th-percentile ISIN) · 2026: 10. As of 2026-01-01 zero of 1,598 ISINs have a filing under 200 days old. The FY2025-26 discovery chunk returned 133 index entries where a full year is ~13,000 — the `corporates-financial-results` feed's behaviour for the recent window needs probing (does it page, cap, or require a different period/date parameterisation?), then a forward sync from 2025-02 to now. Until then the analyst's fundamentals evidence (T1/T2, BC1) is eighteen months stale and the fundamentals backtest arms hold cash after Sep-2025. |
| audit 2026-09-06 | `analyst/monitor/fundamentals.py` re-reads and re-parses the whole PIT store (905k facts, ~17 s) on every `evaluate_segment_revenue_decline` / `segments_disclosed` call. A daily T0 over dozens of holdings × segments is minutes. Cache `read_latest` per as-of date within a run, or serve it from DuckDB with a filing_date predicate. |
| audit 2026-09-06 | Segment names drift across a company's filings — RELIANCE: `Oil & Gas` / `Oil and Gas`, `Organised Retail` / `Organized Retail` / `Retail`; DVL: `Food and Beverage(s)` / `Foods & Beverages`, `Rest of (the) World`. BC1's consecutive-quarter run is keyed on the exact string, so a renamed segment reads as a short run (INTACT with one quarter) rather than a decline. Normalise (case, `&`↔`and`, plural, articles) at the evaluator, keeping the stored key verbatim. |
| M10.4 integrated feed | Two refusal classes the integrated discovery creates by guessing periods from `qe_Date`: (a) the derived `-FY` annual entry for a fourth-quarter record whose document carries no financial-year column (non-March year-ends, quarter-only documents); (b) half-year reporters whose `OneD` column is Oct-Mar, not a quarter. Both are recorded FAILED-retryable on the report (about 4% of units on the first run). Fix: when an entry comes from the integrated feed, let the parser select the shortest undimensioned column ending on `qe_Date` for the quarterly entry and emit the annual entry only when a ~365-day column ends there. |
| M10.4 integrated feed | **DONE 2026-09-06 — two refusal classes of the live campaign were the parser being pedantic, not identity.** Of the first 412 refusals (32 of 114 pages): 87 were over the *spelling* of a scheme already accepted — `in-capmkt:/ScripCode` with a stray colon (53) and SEBI's root under `https://` (30, including `https://www.sebi.gov.in/in-capmkt/ScripCode`, a new scrip scheme) — now folded by `_normalise_scheme`; and 53 were over a stated ISIN with a "different issuer" that was the company's own ISIN mistyped (`INEOFS801015` for `INE0FS801015`, `INE576101022` for `INE576I01022`, `INE903I01010` for `INE093I01010`: 13 of 15 companies). `is_isin_typo_of` accepts a stated ISIN that fails its ISO 6166 check digit or swaps two adjacent characters of the entry's, and logs `xbrl.stated_isin_typo`. Fixtures are the live documents (`tests/fixtures/xbrl/integrated/`, one per variant). Rerunning the campaign window after the driver exits retries every FAILED unit at zero fetch cost — the payloads are in L0. |
| M10.4 integrated feed | **Two companies state a sister company's real ISIN and stay refused.** Ashapura Minechem's documents state `INE569C01020` (Orient Ceratech) and Gillette's state `INE179A01014` (P&G Hygiene) — both real ISINs of other listed companies in the master, so the typo rule rightly does not apply. The document's `Symbol` names the entry's company and its `ISIN` names another; neither side is safe to prefer, so 7 units (3 companies' worth of quarters) are lost per season until a rule is chosen. A defensible one: when the document's symbol matched the entry as-of the filing date *and* the stated ISIN's company has a different symbol in the master on that date, the ISIN is the wrong field — accept, log. Owner's call: it is the first rule that would publish a filing whose own ISIN fact contradicts the join key. |
| M10.4 integrated feed | The DVL/DTIL misattribution is alive on the integrated feed too: 2 documents (4 units) listed under `DVL` (INE477B01010) say `DTIL` in the document and are refused by the as-of symbol check, exactly as designed. Dhunseri Tea's filings are therefore absent from the store under *either* ISIN — NSE lists them under the wrong symbol and the parser will not re-attribute on the document's word alone. A re-attribution path (publish under the document's stated ISIN when it resolves in the master to the company the document names) would recover them; it is the same rule as the row above and should be decided with it. |
| M10.4 integrated feed | 15 documents (30 units) return 404 from `nsearchives.nseindia.com` — "no retry would change" — all filed 2025-07-04. Probably a broken batch at the source; re-check in a later season's rerun before concluding they are gone. |
| M3.1 | **Price backfills have no `--rebuild-from-l0`.** The BSE campaign's L0 (536 sessions, 411 MB) was brought home on 2026-09-06 and every payload re-verified against its sidecar, but the laptop's `prices_raw` cannot be derived from it: `dataplatform.ingest.backfill` always fetches, so deriving the BSE rows locally would re-request 536 sessions (over B1's threshold). The fundamentals runner has the mode; the price runner needs the same — read the L0 ref for `(source, date)` when it exists, fetch only when it does not — so that "bring `data/L0/` home and rebuild" (`ops/runbooks/move-campaign-to-a-server.md`) is true for every source, not only filings. Until then the server's L1 is the only one with BSE rows. |
| audit 2026-09-06 | **The 774 filings whose XBRL document is not in L0 — enumerated, ready to fetch.** `ops/gates/missing-xbrl-payloads-2026-09-06.json` lists every one with its filing id, date, ISIN and document name. They are **536 distinct documents** (one serves ~1.4 index entries, the `_ref_for` reuse M10.4 measured), 153 ISINs, 2018-06 to 2025-02. No re-derivation reaches them — the bytes were never fetched — so this is a scoped re-fetch of a known list: **536 requests, ~22 min** at 2.5 s spacing. Over B1's 200-request threshold. Must hold the `nsearchives.nseindia.com` lease (`dataplatform.ingest.lease`), which means after the integrated campaign on the server finishes. |
| audit 2026-09-06 | **A source for the 593 fund/ETF ISINs is unprobed, and probing it needs the host.** Finding N5: 593 of the 7,536 traded ISINs are `INF`-scheme (ETFs and mutual-fund units) and **zero** are known to D2, because `EQUITY_L.csv` does not list schemes. Action 3 reconstructs *historical* listings and cannot reach them — they need a different source. Candidates to probe and record in `source_register.yaml` the way `ops/gates/source-verification.md` §5.1 does: NSE's ETF list, and the BSE `ListofScripData` call already in the register with a non-`Equity` segment. Deliberately **not** built against a guessed format — "synthetic but taxonomy-shaped" fixtures are where a false-DONE hides (M7.3). Do this *before* Action 3, not after: it is a tenth of the work, removes 593 of the 5,139 unknown ISINs, and stops Action 3 being measured against a target it structurally cannot hit. Closes M5.7 with it (`DEFAULT_PARKING_ISIN = LIQUIDBEES` is a module constant *because* D2 cannot resolve it). |
