# data lake — never committed (L0 is immutable on disk, not in git). Anchored: unanchored `data/`
# also matches package data directories such as dataplatform/ingest/data/, which are checked in.
/data/
# secrets
.env
.env.*
!.env.example
.build_state.lock
ops/build-logs/
ops/backups/
# python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
# artifacts
*.parquet
*.duckdb
.DS_Store
