"""Golden corporate-action cases — one self-contained file per case (EXECUTION_PLAN §4.3).

Each module defines a module-level ``CASE: GoldenCase``. The harness discovers them by walking this
package, so a new case is a new file here and nothing else (M2.6 acceptance 3). See
``tests/golden/README.md`` for the recipe.
"""
